export default function Hero() {
  return (
    <section className="hero">
      <h1>You don’t need a lifetime decision. Just a moment.</h1>
      <p>
        JustOnce exists for the moment between impulse and action.
        Where awareness can quietly change what comes next.
      </p>
    </section>
  );
}
